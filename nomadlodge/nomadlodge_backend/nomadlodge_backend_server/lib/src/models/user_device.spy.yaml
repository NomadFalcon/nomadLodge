class: UserDevice 
table: user_device
fields:
  token: String
  plaform: String
  osVersion: String
  deviceInfo: String?
  user: User?, relation
indexes:
  userdevice_user_unique_idx:
    fields: userId
    unique: true
