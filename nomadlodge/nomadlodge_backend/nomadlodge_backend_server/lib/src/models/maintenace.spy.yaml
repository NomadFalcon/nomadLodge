class: Maintenance
table: maintenance
fields:
  maintenancetype: MaintenanceType
  start: DateTime
  end: DateTime?
  description: String
  tasks: List<Task>?, relation
  user: User?, relation(optional)
  fee: Fee?, relation(optional)
  location: Location?, relation
  booking: Booking?, relation(optional)
indexes:
  maintenance_user_unique_idx:
    fields: userId
    unique: false
  maintenance_fee_idx:
    fields: feeId
    unique: false
  maintenance_location_unique_idx:
    fields: locationId
    unique: false
  maintenance_booking_unique_idx:
    fields: bookingId
    unique: false
