class: Integration 
table: integration
fields:
  apiKey: String
  integrationType: IntegrationType
  lastReloadDate: DateTime?
  user: User?, relation
indexes:
  integration_user_unique_idx:
    fields: userId
    unique: true